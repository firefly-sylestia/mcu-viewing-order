// Native browser scroll — no JS wheel interception.
// Browser handles momentum, kinetic scrolling, and overscroll natively
// with zero JS overhead on every scroll tick.
export const useLenis = () => {};
